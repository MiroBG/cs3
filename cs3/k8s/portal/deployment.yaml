apiVersion: apps/v1
kind: Deployment
metadata:
  name: cs3-portal
  namespace: cs3-prod
  labels:
    app: cs3-portal
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "5000"
    prometheus.io/path: "/metrics"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cs3-portal
  template:
    metadata:
      labels:
        app: cs3-portal
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: cs3-portal
      imagePullSecrets:
      - name: ecr-registry
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: portal
        image: PORTAL_ECR_IMAGE
        imagePullPolicy: Always
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        ports:
        - containerPort: 5000
          name: http
        env:
        - name: FLASK_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: portal-secrets
              key: flask-secret-key
        - name: COGNITO_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: portal-secrets
              key: cognito-client-id
        - name: COGNITO_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: portal-secrets
              key: cognito-client-secret
        - name: COGNITO_DOMAIN
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: cognito-domain
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: portal-secrets
              key: db-host
        - name: DB_NAME
          value: employees
        - name: DB_USER
          value: postgres
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: portal-secrets
              key: db-password
        - name: DB_PORT
          value: "5432"
        - name: PORTAL_URL
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: portal-url
        - name: PORTAL_DEMO_AUTH
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: portal-demo-auth
        - name: AWS_REGION
          value: eu-central-1
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        readinessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 30
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: portal-config
  namespace: cs3-prod
data:
  cognito-domain: "COGNITO_DOMAIN_VALUE"
  portal-url: "PORTAL_URL_VALUE"
  portal-demo-auth: "PORTAL_DEMO_AUTH_VALUE"
---
apiVersion: v1
kind: Secret
metadata:
  name: portal-secrets
  namespace: cs3-prod
type: Opaque
stringData:
  flask-secret-key: "FLASK_SECRET_KEY_VALUE"
  cognito-client-id: "COGNITO_CLIENT_ID_VALUE"
  cognito-client-secret: "COGNITO_CLIENT_SECRET_VALUE"
  db-host: "DB_HOST_VALUE"
  db-password: "DB_PASSWORD_VALUE"
